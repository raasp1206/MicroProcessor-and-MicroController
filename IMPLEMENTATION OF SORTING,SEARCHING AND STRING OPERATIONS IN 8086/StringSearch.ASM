                OUTPUT 2500AD
.ORG 2000H
DATA SEGMENT
ARRAY DW 0001H,0003H,0004H,0005H,0007H
COUNT EQU 0005H
KEY DW 0004H
RES DB 00H
DATA ENDS
CODE SEGMENT
        ASSUME CS:CODE,DS:DATA
        MOV AX,DATA
        MOV DS,AX
        MOV CX,COUNT
        MOV SI,OFFSET ARRAY
        MOV RES,00H
L1:     MOV AX,[SI]
        CMP AX,KEY
        JE L2
        INC SI
        INC SI
        LOOP L1
        HLT
L2:     MOV RES,01H
        HLT
CODE ENDS
ENDS
